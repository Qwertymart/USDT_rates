run:
  timeout: 5m
  tests: true

linters:
  disable-all: true
  enable:
    - gofmt       
    - goimports   
    - errcheck    
    - ineffassign 
    - govet       
    - gosimple    
    - staticcheck 
    - gosec
    - gocritic